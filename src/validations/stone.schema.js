// import { z } from "zod";

// export const searchStoneSchema = z.object({
//   keyword: z.string().min(1, "keyword is required"),
//   category: z.enum(["LOVE", "MONEY", "WORK", "HEALTH", "SPIRITUAL"]).optional(),
// });
